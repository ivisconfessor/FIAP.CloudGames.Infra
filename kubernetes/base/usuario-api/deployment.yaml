apiVersion: apps/v1
kind: Deployment
metadata:
  name: usuario-api-deployment
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: usuario-api
  template:
    metadata:
      labels:
        app: usuario-api
    spec:
      containers:
      - name: usuario-api
        image: southamerica-east1-docker.pkg.dev/fiap-cloud-games/fiap-cloudgames/usuario-api:latest
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        envFrom:
        - configMapRef:
            name: services-config
        - secretRef:
            name: jwt-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
