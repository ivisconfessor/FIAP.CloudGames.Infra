apiVersion: v1
kind: Service
metadata:
  name: jogo-api-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: jogo-api
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
